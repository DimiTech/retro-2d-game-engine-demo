# retro-2d-game-engine-demo

1. Place the two projects next to each other:

```bash
$ tree -L 1
.
├── retro-2d-game-engine
└── retro-2d-game-engine-demo
```

2. Build the game in `retro-2d-game-engine`

3. Update the demo project:

```bash
./update.sh
```
